#define _CRT_SECURE_NO_WARNINGS 1

#include <stdio.h>
#include <stdbool.h>

int main()
{
	while (true) //It's also allowed to use 1, without even haivng to include "stdbool.h".
	{
		printf("meow\n");
	}
}